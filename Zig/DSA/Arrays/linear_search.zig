const std = @import("std");
print = std.debug.print;

pub fn main() !void {
    const arr: []const u8{ 12, 8, 9, 11, 5, 11;
    var len: i32 = arr.len;

    const target:i32 = 5;

    const idx = linear_search(arr);

}

fn linearSearch() {
    
}